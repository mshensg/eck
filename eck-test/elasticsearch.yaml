---
apiVersion: elasticsearch.k8s.elastic.co/v1
kind: Elasticsearch
metadata:
  namespace: eck-test
  name: es-eck
spec:
  version: 8.19.2
  monitoring:
    metrics:
      elasticsearchRefs:
      - name: es-eck
    logs:
      elasticsearchRefs:
      - name: es-eck
  nodeSets:
    - name: hot
      count: 15
      config:
        node.roles: ["remote_cluster_client","ml","ingest","data"]
        node.store.allow_mmap: false
        xpack.security.audit.enabled: true
      podTemplate:
        spec:
          containers:
          - name: elasticsearch
            resources:
              requests:
                memory: 3Gi
                cpu: "1"
              limits:
                memory: 3Gi
                cpu: "1"
      volumeClaimTemplates:
      - metadata:
          name: elasticsearch-data
        spec:
          accessModes:
          - ReadWriteOnce
          resources:
            requests:
              storage: 10Gi
          storageClassName: ebs-gp3

    - name: master-only
      count: 3
      config:
        node.roles: ["master","remote_cluster_client","data_content"]
        node.store.allow_mmap: false
        xpack.security.audit.enabled: true
      podTemplate:
        spec:
          containers:
          - name: elasticsearch
            resources:
              requests:
                memory: 2Gi
                cpu: 1
              limits:
                memory: 2Gi
                cpu: 1
      volumeClaimTemplates:
      - metadata:
          name: elasticsearch-data
        spec:
          accessModes:
          - ReadWriteOnce
          resources:
            requests:
              storage: 5Gi
          storageClassName: ebs-gp3
